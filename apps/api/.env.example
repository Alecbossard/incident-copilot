# Local dev config for Incident Co-Pilot API

# Postgres running on your machine (outside Docker)
DATABASE_URL=postgresql://copilot:copilot@localhost:5432/copilot

# Optional: Redis (not used yet but ready)
REDIS_URL=redis://localhost:6379

# Python AI microservice when running locally (without Docker)
# Example: `uvicorn main:app --reload --port 8000` in apps/ai
AI_BASE=http://localhost:8000

# CORS origin for the web app in local dev
CORS_ORIGIN=http://localhost:3000

# Shared API key (must match web/.env.local and Docker compose)
API_KEY=dev-secret-123

# Allow or forbid PATCH /incidents/:id from the UI
ALLOW_STATUS_PATCH=false

# Embeddings & summaries (Gemini optional)
# If EMB_FALLBACK=local, embeddings are computed locally (no external API).
EMB_FALLBACK=local
SUMMARY_USE_GEMINI=false
# GEMINI_EMBED_MODEL=text-embedding-004
# GEMINI_API_KEY=your-gemini-api-key-here
